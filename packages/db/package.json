{
  "name": "@edubeam/db",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push --skip-generate --accept-data-loss",
    "reset": "prisma db push --force-reset --skip-generate",
    "seed": "tsx src/seed.ts",
    "seed:force": "tsx src/seed.ts --force",
    "import-students": "tsx src/import-students.ts",
    "migrate-blocks": "tsx src/migrate-blocks.ts",
    "bulk-logins": "tsx src/bulk-logins.ts",
    "use-postgres": "node scripts/use-postgres.mjs",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.9.0",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2"
  }
}
