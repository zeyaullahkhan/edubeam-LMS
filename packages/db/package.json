{
  "name": "@edubeam/db",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push --skip-generate",
    "reset": "prisma db push --force-reset --skip-generate",
    "seed": "tsx src/seed.ts",
    "seed:force": "tsx src/seed.ts --force",
    "use-postgres": "node scripts/use-postgres.mjs",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.9.0",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2"
  }
}
